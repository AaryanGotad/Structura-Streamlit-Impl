[project]
name = "structura"
version = "0.1.0"
description = "Structura Streamlit app."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "streamlit>=1.35.0,<2",
    "Pillow>=10.0",
    "tensorflow-cpu>=2.16.1",
    "keras-hub",
    "spacy",
]

[tool.uv]
package = false
